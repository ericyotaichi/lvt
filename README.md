# LVT Starter v2

Quickstart project with Laravel + Vue + Tailwind + Docker.

# 若剛剛有改 docker/php/Dockerfile，先重建一次 app
docker compose build app

# 允許當前視窗執行腳本
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 一鍵啟動
.\scripts\bootstrap.ps1